---
// This is the "frontmatter" section - it runs on the server at build time
// You can import components, fetch data, or define variables here
const pageTitle = "allen";
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{pageTitle}</title>
	</head>
	<body>
		<!-- Navigation -->
		<nav>
			<a href="/" class="nav-link active">home</a>
			<a href="/about" class="nav-link">about me</a>
			<a href="/writings" class="nav-link">writings</a>
			<a href="/random" class="nav-link">random</a>
		</nav>

		<!-- Main Content -->
		<main>
			<h1>hi, i'm allen</h1>

			<p>
				i'm 23yo, based in South Bay, but moving to SF soon!
				i just moved back from Taiwan, where I completed 6 months of mandatory military service
			</p>

			<section class="interest-section" aria-label="current interests">
				<div>
					<h2>i'm currently...</h2>
					<ul class="interest-list">
						<li>
							learning machine learning by watching Karpathy's
							<a href="https://karpathy.ai/zero-to-hero.html">Zero to Hero</a> series and building deep understanding through
							<a href="https://github.com/kshitijg92/karpathy-zero-to-hero-deep-work">notebooks with detailed notes</a>,
							auditing <a href="https://cs336.stanford.edu/">CS 336</a> and
							<a href="https://cs221m.github.io/">CS 221M</a>, and reading interp and alignment papers from
							<a href="https://www.anthropic.com/research">Anthropic</a> and
							<a href="https://www.goodfire.ai/research">Goodfire</a>
						</li>
						<li>
							exploring Stanford's campus to find the best study spots (ideally with monitors), secret cozy & quiet places to nap, and occassionally going to SF and working from
							<a href="https://human.capital/">human capital</a> office as part of their program
						</li>
						<li>
							reading Theo Baker's
							<a href="https://www.goodreads.com/book/show/24970115-how-to-rule-the-world?ac=1&amp;from_search=true&amp;qid=sUaWS58WzJ&amp;rank=1">How to Rule the World</a>
						</li>
						<li>
							watching the NBA playoffs,
							<a href="https://www.youtube.com/watch?v=IU8mjG5xtvU">House of Feelings</a>
						</li>
						<li>
							3 tabling microstakes on <a href="https://purepoker.us/">Pure Poker</a> and trying to find good home games
						</li>
					</ul>
				</div>

				<div>
					<h2>i'm excited about...</h2>
					<ul class="interest-list">
						<li>
							neurotech advancements like the
							<a href="https://science.xyz/technologies/prima/">PRIMA visual prosthesis</a>,
							the <a href="https://science.xyz/technologies/biohybrid/">biohybrid BCI</a>, and other
							<a href="https://neuralink.com/technology/">high bandwidth BCIs</a>
						</li>
						<li>
							applying AI in bio and life sciences
						</li>
						<li>
							meeting cool people at 1517 Summit and Monaco Poker Invitational
						</li>
						<li>
							celebrating the US Semiquincentennial (fancy name for 250th anniversary) on Jul 4 in DC with family
						</li>
						<li>
							going to my first wedding in the US and being a groomsman for the first time!
						</li>
						<li>
							going to Burning Man for the first time, my group and i have no idea what we are doing, advice would be greatly appreciated
						</li>
					</ul>
				</div>
			</section>

			<p>
				most recently I founded and built <a href="https://codedreams.app/">Codedreams</a>, an AI tutor for iOS devs
			</p>
			<p>
				before that, I did research with intracortical BCIs at <a href="https://www.braingate.org/">BrainGate</a>
			</p>
			<p>
				and i worked on cool poker & tech stuff at <a href="https://www.rf.team/">RF Labs</a>
			</p>
			<p>
				say hi on <a href="https://x.com/allenysu">X</a> or allensu [at] gatech.edu
				i always love meeting new ppl!
			</p>

			<!-- Social Links -->
			<div class="social-links">
				<a href="https://www.linkedin.com/in/allensu02/" class="social-link">
					<span class="arrow">↗</span> linkedin
				</a>
				
				<a href="https://github.com/allensu02" class="social-link">
					<span class="arrow">↗</span> github
				</a>

				<a href="https://www.goodreads.com/user/show/117840634-allen-su" class="social-link">
					<span class="arrow">↗</span> goodreads
				</a>
			</div>

			<p class="age-line">
				born <span id="live-age" data-live-age>23 yr 8 mo</span> ago in LA, CA 🌴
			</p>

			<section class="news-section" aria-label="news">
				<h2>news</h2>
				<ul class="news-list">
					<li>
						<time datetime="2026-02-21">Feb 21, 2026</time>
						<span>
							won 1st place ($16k USD cash) at
							<a href="https://humansand.ai/">humans&amp;</a>
							<a href="https://luma.com/2pbif8t9?tk=3Ptii9">hackathon</a>
						</span>
					</li>
				</ul>
			</section>
		</main>

		<script>
			const birth = {
				year: 2002,
				month: 9,
				day: 10,
				hour: 9,
				minute: 10,
				second: 0,
			};

			const ageElement = document.querySelector("[data-live-age]");
			const losAngelesFormatter = new Intl.DateTimeFormat("en-US", {
				timeZone: "America/Los_Angeles",
				year: "numeric",
				month: "numeric",
				day: "numeric",
				hour: "numeric",
				minute: "numeric",
				second: "numeric",
				hourCycle: "h23",
			});

			const getLosAngelesParts = (date) => {
				const parts = Object.fromEntries(
					losAngelesFormatter
						.formatToParts(date)
						.filter((part) => part.type !== "literal")
						.map((part) => [part.type, Number(part.value)])
				);

				return {
					year: parts.year,
					month: parts.month,
					day: parts.day,
					hour: parts.hour,
					minute: parts.minute,
					second: parts.second,
				};
			};

			const daysInMonth = (year, month) => new Date(Date.UTC(year, month, 0)).getUTCDate();

			const getAgeParts = () => {
				const now = getLosAngelesParts(new Date());

				let years = now.year - birth.year;
				let months = now.month - birth.month;
				let days = now.day - birth.day;
				let hours = now.hour - birth.hour;
				let minutes = now.minute - birth.minute;
				let seconds = now.second - birth.second;

				if (seconds < 0) {
					seconds += 60;
					minutes -= 1;
				}

				if (minutes < 0) {
					minutes += 60;
					hours -= 1;
				}

				if (hours < 0) {
					hours += 24;
					days -= 1;
				}

				if (days < 0) {
					const previousMonth = now.month === 1 ? 12 : now.month - 1;
					const previousMonthYear = now.month === 1 ? now.year - 1 : now.year;

					days += daysInMonth(previousMonthYear, previousMonth);
					months -= 1;
				}

				if (months < 0) {
					months += 12;
					years -= 1;
				}

				return { years, months, days, hours, minutes, seconds };
			};

			const renderAge = () => {
				if (!ageElement) return;

				const { years, months, days, hours, minutes, seconds } = getAgeParts();

				ageElement.textContent = `${years} yr ${months} mo ${days} day ${hours} hr ${minutes} min ${seconds} sec`;
			};

			renderAge();
			setInterval(renderAge, 1000);
		</script>
	</body>
</html>

<style>
	/*
	 * GLOBAL STYLES
	 * We define CSS variables (custom properties) at the :root level
	 * This makes it easy to change colors across the entire site
	 */
	:root {
		--bg-color: #000000;
		--text-color: #ffffff;
		--link-color: #5EEAD4;        /* Mint/Teal - your choice! */
		--text-muted: #9CA3AF;        /* Gray for subtle text */
	}

	/*
	 * Base styles for the HTML element
	 * We use a system font stack for fast loading and native feel
	 */
	html {
		font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
		background-color: var(--bg-color);
		color: var(--text-color);
	}

	/*
	 * Body reset and layout
	 * max-width constrains content for readability (long lines are hard to read)
	 * margin: 0 auto centers the content horizontally
	 */
	body {
		margin: 0;
		padding: 2rem;
		max-width: 650px;
		margin: 0 auto;
		line-height: 1.6;             /* Spacing between lines - improves readability */
	}

	/*
	 * NAVIGATION
	 * display: flex puts items in a row
	 * gap adds space between flex items (cleaner than margins)
	 */
	nav {
		display: flex;
		flex-wrap: wrap;
		gap: 2rem;
		margin-bottom: 3rem;
	}

	.nav-link {
		color: var(--text-muted);
		text-decoration: none;        /* Removes the underline */
		font-size: 0.95rem;
	}

	.nav-link:hover,
	.nav-link.active {
		color: var(--text-color);     /* White when hovered or active */
	}

	/*
	 * TYPOGRAPHY
	 * The heading uses a larger font-weight for that bold look
	 */
	h1 {
		font-size: 2rem;
		font-weight: 700;
		margin-bottom: 1.5rem;
		letter-spacing: -0.02em;      /* Slightly tighter letter spacing looks modern */
	}

	p {
		margin-bottom: 1rem;
		color: var(--text-color);
	}

	.interest-section {
		display: grid;
		gap: 2rem;
		margin: 2.5rem 0;
	}

	.interest-section h2 {
		color: var(--text-muted);
		font-size: 1.05rem;
		font-style: italic;
		font-weight: 700;
		margin: 0 0 1rem;
	}

	.interest-list {
		list-style: disc;
		margin: 0;
		padding-left: 1.25rem;
	}

	.interest-list li {
		color: var(--text-color);
		margin-bottom: 0.9rem;
	}

	@media (min-width: 900px) {
		.interest-section {
			grid-template-columns: repeat(2, minmax(0, 1fr));
			margin-left: 50%;
			transform: translateX(-50%);
			width: min(calc(100vw - 4rem), 1100px);
		}
	}

	/*
	 * LINKS
	 * Inline links get the mint/teal color
	 */
	a {
		color: var(--link-color);
		text-decoration: none;
	}

	a:hover {
		text-decoration: underline;   /* Underline on hover gives feedback */
	}

	/*
	 * SOCIAL LINKS SECTION
	 * Another flex container for the bottom links
	 */
	.social-links {
		display: flex;
		gap: 1.5rem;
		margin-top: 2rem;
	}

	.social-link {
		color: var(--link-color);
		font-size: 0.95rem;
		display: flex;
		align-items: center;
		gap: 0.25rem;
	}

	.social-link:hover {
		color: var(--link-color);
		text-decoration: underline;
	}

	.arrow {
		font-size: 0.85rem;
	}

	.age-line {
		color: var(--text-muted);
		font-size: 0.95rem;
		margin-top: 1.25rem;
	}

	#live-age {
		color: var(--text-color);
		font-variant-numeric: tabular-nums;
	}

	.news-section {
		margin-top: 2.25rem;
	}

	.news-section h2 {
		font-size: 1.05rem;
		font-weight: 700;
		margin: 0 0 0.9rem;
	}

	.news-list {
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.news-list li {
		display: grid;
		gap: 0.35rem;
		margin-bottom: 0.75rem;
	}

	.news-list time {
		color: var(--text-muted);
		font-size: 0.9rem;
		font-variant-numeric: tabular-nums;
	}

	@media (min-width: 560px) {
		.news-list li {
			grid-template-columns: 7.25rem 1fr;
		}
	}
</style>
